import React from 'react'

const LoadingSpinner = () => {
    return (
        <div className="col text-center fade-in slow" style={{ padding: '64px 0' }}>
            <div className="loader" />
        </div>
    )
}

export default LoadingSpinner
